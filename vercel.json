{
  "version": 2,
   "buildCommand": "pnpm install && pnpm run build",
  "installCommand": "pnpm install",
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.ts"
    }
  ]
}